The `cd-cli-app.descriptor.json` file provides a detailed "biological" snapshot of the `cd-cli` system, mapping its physical files to the architectural roles defined in your interfaces. Based on the provided `CdAppDescriptor` and `DirectoryNode` specifications, here is the analysis of the current state of the system:

### **1. System Health & Compliance Metrics**
The scanner has computed a significant divergence between the physical code and the intended architectural DNA:

* **Compliance Ratio (CR): 0.511 (51.1%)**: Only about half of the nodes in the system currently align with the strict Corpdesk structural conventions.
* **Infection Ratio ($I$): 0.488 (48.8%)**: Nearly 49% of the project is considered "foreign" or non-compliant by the current scanning rules.

### **2. Analysis of "Structural Infection" ($\chi$)**
The high infection ratio is largely driven by naming convention violations that prevent the `RoleResolver` from correctly attributing weights to nodes.

* **Naming Collisions:** Files like `calendarcontroller.ts` and `schedulercontroller.ts` are flagged as **`isCdForeign: true`**. According to the standard (RFC-0001), these should follow the `<name>.<role>.ts` pattern (e.g., `calendar.controller.ts`).
* **Directory Misplacement:** Entire directories, such as certain `controllers` and `models` folders, have been assigned a weight of `1` and marked as foreign. This indicates they are residing in paths that the `SeedConfig` does not recognize as valid module sub-structures.


### **3. Role Attribution & Weights**
In the compliant sections of the descriptor, we can see the mathematical weighting system at work:

* **Models:** Nodes correctly identified as `cdObjRoleName: "model"` are assigned a **weight of 8** and marked as compliant. This includes files like `coop-stat.model.ts` and `coop-stat-view.model.ts`.
* **Root Structure:** The root node (labeled `cd-cli`) serves as the base for the `DirectorySignatureDescriptor`, with a recursive child structure that maps the entire `src/CdCli/app` hierarchy.

### **4. Identified "Foreign Bodies" in the Workspace**
The scanner has flagged several "noise" files that contribute to the infection ratio:
* `tsconfig.json` and `tsconfig_original.json` are marked as **`isCdForeign: true`**. While necessary for TypeScript, they are not considered "Corpdesk Objects" ($\chi$) in the context of the application's functional logic.
* The `sdk/` directory contains files like `acl.service.js`, which are identified as services but may be flagged if they reside outside the standard `src/<AppName>/sys` or `src/<AppName>/app` boundaries.


### **Summary for Evolution**
This descriptor confirms that the `cd-cli` "organism" is currently in a state of **architectural drift**. To improve the Compliance Ratio, the next "Genesis" or "Repair" cycle should focus on:
1.  **Renaming** files to include the dot-separator for roles (e.g., `.controller.ts`).
2.  **Aligning** foreign directories with the path logic defined in the `SeedConfig`. 

**This machine-readable model ($\Gamma$) is now ready to be used as a prompt seed for an AI agent to perform an automated refactoring of the identified foreign nodes.**